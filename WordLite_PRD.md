# 英语单词学习 App PRD（WordLite）

## 1. 产品概述

### 1.1 产品名称

WordLite（暂定）

### 1.2 产品定位

面向小学生的无痛英语单词学习工具，通过固定学习节奏 + 间隔重复 + 轻游戏化机制，让用户在短时间内持续记忆单词。

### 1.3 目标用户

- 年龄：6～12 岁小学生
- 使用设备：个人安卓手机（竖屏单手操作优先）

### 1.4 术语

| 术语 | 含义 |
|------|------|
| 一词 / 完成一词 | 某个单词在当日会话中走完 **四步学习流程** 并成功结算 |
| 今日会话 / 当日队列 | 由「新词段 + 复习段」组成的今日学习列表；可因中断产生检查点 |
| 整段会话完成 | 当日队列中 **最后一个词** 四步通关并结算，本会话结束（检查点清空） |
| 到期复习 | 已到 `nextReviewAt` 对应日期、且阶段未到 `mastered` 的单词 |

------------------------------------------------------------------------

## 2. 产品目标

### 2.1 用户目标

- 每日学习时长 ≤ 5 分钟
- 提高学习完成率（≥ 70%，后续可用埋点验证）

### 2.2 学习目标

- 实现「认识 + 会用」
- 持续实现「比昨天更好」（见 4.4）

------------------------------------------------------------------------

## 3. 功能范围

### 3.1 本期功能（MVP）

- 单词学习流程（四步）
- 间隔重复复习系统（阶段 + 复习日）
- 每日任务生成（新词 + 复习队列）
- 学习中断恢复（会话检查点 + 切后台落盘）
- 奖励系统（能量、碎片、皮肤档位）
- 家长观察页面（统计只读展示）
- 本地存储（离线可用）
- **可扩展词库**：内置词条 + 随应用打包的 **JSON 词包** 合并；维护者可用 **CSV→JSON 工具** 扩展（见 §4.5）

### 3.2 明确不在 MVP 范围（可列 backlog）

- 账号登录、云同步、多设备
- **应用内**从用户设备任意路径**选择文件导入词库**、教师端（随包 JSON / 发版更新词库不在此列）
- 推送提醒、社交排行
- **独立「设置」页**（含朗读 / 系统 TTS 等）：学习页不提供此类入口，避免干扰专注；**需求草案见 3.3**。

### 3.3 未来功能：设置（规划中）

**定位**：集中放置**与学习四步无关**的系统级或偏好项；**不在**学习流程页（尤其「听音选词」）常驻 TTS 或系统朗读类配置控件。

**建议入口**：首页、个人区或 AppBar 二级入口等（具体 IA 待产品定稿）；与「开始学习」「继续学习」主路径解耦。

**拟定范围（可随迭代裁剪）**

| 模块 | 说明 |
|------|------|
| 朗读与 TTS（Android） | 展示本机已安装的 TTS 引擎列表与当前默认包名（复用原生 `getTtsEngines`、`getDefaultTtsEnginePackage`）；提供跳转系统「文字转语音 / 无障碍 / 设置」（`openSystemTtsSettings`）；文案需说明：**应用内朗读仅使用本机引擎，不修改系统全局默认**（除非用户自行在系统设置中更改）。 |
| 其他（占位） | 例如主题/音效、家长锁、清空学习缓存等；待与整体信息架构一并梳理后再拆条。 |

**技术备忘**：Flutter 侧通过 `MethodChannel('com.wordlite.app/android_tts')` 调用 `android/.../WordLiteAndroidTtsPlugin.kt` 中已实现的方法；与 `LearnScreen` 内仅使用 `prepare` / `speak` / `stop` 的分工保持一致。

------------------------------------------------------------------------

## 4. 核心功能设计

### 4.1 学习系统

#### 4.1.1 每日任务结构

- **新词**：每日最多 **5** 个；从内置词库按固定顺序取「尚未出现在学习进度中」的词，直至凑满 5 个或词库用尽。
- **复习**：从「当日到期」的单词中随机抽取，数量 **不超过 8**；实际数量 = `min(8, 当日到期单词数)`。  
  - **产品决策：复习不必须凑满 5 个**——到期词少于 5 时，**不**用未到期词硬凑；「5～8 个」仅表示在到期量充足时，复习段自然可落在约 5～8 个（且不超过 8）。
- **队列顺序**：先新词、后复习（同一日会话内顺序学习）。

#### 4.1.2 学习流程（四步，须按序完成）

1. 看图识词  
2. 听音选词（系统 TTS 朗读英文，环境需允许发声）  
3. 看词选图  
4. 例句填空（给出英文例句，目标词位置留空 `___`，从若干**英文**候选项中选词填入；候选项可含不同时态、单复数或其它干扰形式）

**规则**：单步答错时，该词 SRS 按 4.2 回退，但**仍停留在当前步骤**，直至该步答对方可进入下一步（避免跳过训练）。

#### 4.1.3 学习中断与恢复

- 开始或恢复今日学习时，生成/沿用 **当日会话检查点**（包含：日期、队列、当前词下标、当前步骤下标）。
- 用户离开学习页、应用进入后台等场景，应尽可能 **持久化检查点与进度**，返回后可「继续学习」；可选提供「放弃今日会话/清空检查点」类操作（以实际界面为准）。

### 4.2 复习系统（SRS）

实现说明与代码映射见 `docs/superpowers/specs/2026-05-12-wordlite-prd-4.2-srs-design.md`；核心规则在 `lib/services/review_srs.dart`。

#### 4.2.1 阶段状态

正向顺序：

`learning` → `review_1` → `review_3` → `review_7` → `review_14` → `mastered`

#### 4.2.2 复习时间安排（自然日）

- 进入 `review_1` / `review_3` / `review_7` / `review_14` 后，分别自**通关当日**起算，下一次可复习日为 **+1 / +3 / +7 / +14** 个自然日（与实现中「按日期零点」判断到期一致）。
- `mastered` 不再安排复习日。

#### 4.2.3 错误回退（任一步答错触发）

对当前阶段执行回退：

`review_14` → `review_7` → `review_3` → `review_1` → `learning`

**补充**：若当前已为 `mastered`（例如复习会话中再练），答错时回退到 `review_14`，再按上表继续回退（与实现一致，避免「已掌握」无惩罚）。

**入队口径**：答错回退至任一 `review_*` 后，该词应按 PRD 4.2.4 **当日可复习** 再次参与复习抽样；`learning` 仍不设 `nextReviewAt`。

#### 4.2.4 到期判定（产品口径）

- 「当日可复习」：词的 `nextReviewAt` 所指日期 **不晚于** 今天（自然日），且阶段未到 `mastered`。

#### 4.2.5 验收勾选

> 自动化证据：`flutter test` 含 `test/review_srs_test.dart`、`test/word_lite_repository_test.dart`；规则见 `lib/services/review_srs.dart`。

- [x] **4.2.1** 答对后阶段按 `learning` → … → `mastered` 正向推进  
- [x] **4.2.2** 进入 `review_1` / `review_3` / `review_7` / `review_14` 后，`nextReviewAt` 分别为通关自然日 +1 / +3 / +7 / +14；`mastered` 无复习日  
- [x] **4.2.3** 答错回退链；`mastered` 答错 → `review_14`  
- [x] **4.2.3 入队口径** 答错回退至 `review_*` 后 `nextReviewAt` 与当日可复习一致；回退至 `learning` 时清空 `nextReviewAt`  
- [x] **4.2.4** 「当日可复习」判定与复习段抽样一致（`mastered` 不进复习段）  
- [ ] **手测（可选）** 多阶段词在答错、换日后，队列与家长统计观感符合 PRD 叙述  

### 4.3 奖励系统

实现说明与代码映射见 `docs/superpowers/specs/2026-05-12-wordlite-prd-4.3-rewards-design.md`；结算逻辑在 `lib/services/word_lite_repository.dart`。

- **能量**：每 **完成一词**（四步通关并结算）**+1** 能量。
- **碎片**：按 **整段会话** 发放——当用户完成 **当日队列中全部词**（最后一词四步通关）时，发放 **+1** 碎片（见术语「整段会话完成」）。中途退出未跑完队列则不发本次会话碎片。
- **皮肤**：累计 **10** 碎片可解锁下一档皮肤主题；共 **3** 档（具体表现以界面为准）。

#### 4.3.5 验收勾选

> 自动化证据：`flutter test` 含 `test/word_lite_repository_test.dart`（能量、碎片会话边界、皮肤解锁）；常量见 `WordLiteRepository.shardsPerSessionCompleted` / `shardsPerSkin` / `skinLevels`。

- [x] **能量** 每完成一词（四步通关）+1  
- [x] **碎片** 仅整段会话完成（当日队列最后一词四步成功）发放；多词队列中途完成不发放  
- [x] **皮肤** 每满 10 碎片解锁下一档，最多 3 档；解锁时扣减碎片（与单测「9+1」场景一致）  
- [ ] **手测（可选）** 首页与家长页能量/碎片/主题展示与上述计数一致；换日后再完成一组会话观感正常  

### 4.4 家长观察

实现说明与代码映射见 `docs/superpowers/specs/2026-05-12-wordlite-prd-4.4-parent-stats-design.md`；统计在 `lib/services/word_lite_repository.dart`，只读页 `lib/screens/parent_screen.dart`。

展示维度（只读）：

- **今日学习**：今日已完成词数（按「四步通关」计数）。
- **累计掌握**：已进入 `mastered` 的词数量。
- **连续天数**：有学习行为的自然日连续天数（同日多次学习不重复 +1）。
- **比昨天提升**：今日累计掌握数相对「昨日换日快照」的增量（用于「比昨天更好」）。

#### 4.4.5 验收勾选

> 自动化证据：`flutter test` 含 `test/word_lite_repository_test.dart`（换日 / 昨日快照、连续日 streak、同日不重复、间隔重置）、`test/parent_screen_test.dart`（四维只读展示）。

- [x] **今日学习** 与四步通关计数一致，换日后归零（`_rollDailyIfNeeded`）  
- [x] **累计掌握** 为 `mastered` 词数量（`_countMastered`）  
- [x] **连续天数** 连续自然日 +1；同日多次不重复 +1；间隔超过 1 天重置为 1（`_touchStreak`）  
- [x] **比昨天提升** 为「当前累计掌握 − 换日写入的昨日快照」（`UserStats.deltaVsYesterday`）  
- [ ] **手测（可选）** 家长页与换日后「今日 / 连续 / 比昨天」观感与 PRD 叙述一致  

### 4.5 词库与内容

- **内置词表**：默认 **离线** `WordEntry` 列表（含英文、释义、配图占位 emoji、完整例句、第四步挖空 `___`、英文填空候选项与干扰项等）。
- **扩展词包**：支持随应用打包的 **JSON 词包**（`assets/word_packs/`），启动时 **合并** 进词表（内置顺序在前，扩展在后，新词仍按该顺序取用）。详见 `docs/superpowers/specs/2026-05-12-wordlite-prd-4.5-word-bank-design.md`。
- **年级标签**：扩展字段 **`gradeTag`**（可选，如「一年级」）供分册/分年级维护；**本期不实现**按年级过滤学习队列。
- **外部文档工作流**：维护者可用 **管道符 `|` 分隔的 CSV**，经仓库内工具 `dart run tool/word_pack_csv_to_json.dart` 转为 JSON 词包再放入 assets（见 `assets/word_packs/README.md`）。
- **不在本期**：应用内任意路径选文件导入、云同步、无校验的第三方格式自动适配。

#### 4.5.5 验收勾选

> 自动化证据：`flutter test` 含 `test/word_pack_loader_test.dart`、`test/word_entry_json_test.dart`；启动合并见 `WordBank.loadEmbeddedPacks` 与 `main.dart`。

- [x] **内置词表** 仍可离线学习四步  
- [x] **JSON 词包** 启动合并；与内置 **id 冲突** 的扩展条目被跳过  
- [x] **解析校验** 缺字段、无 `___`、干扰项不足 3 条等拒绝入库（单测）  
- [x] **CSV→JSON 工具** 可生成合法词包文件（脚本存在且 `dart run` 可用）  
- [ ] **手测（可选）** 安装包内含示例扩展词，新词队列在掌握内置后出现扩展词；TTS/四步表现正常  

------------------------------------------------------------------------

## 5. 技术要求

- **客户端**：Flutter；目标平台 **Android**（最低 SDK 版本以工程 `android/app/build.gradle` 为准）。
- **本地存储**：`SharedPreferences`（或等价方案）持久化学习进度、检查点、奖励与统计；**无后端依赖**。
- **离线**：核心学习路径不依赖网络；TTS 使用设备本地引擎（失败时应有降级提示或重试，具体交互可在实现迭代）。
- **无障碍与儿童友好**：字号与触控区域适合小学生；关键操作有明确反馈。

------------------------------------------------------------------------

## 6. MVP 验收标准（建议）

- `flutter analyze` 无错误；`flutter test` 全绿（含仓库层关键业务单测）。
- 能完成：生成当日队列 → 四步学习 → 答错回退与答对进阶 → 退出再进入可恢复检查点（同日）。（检查点恢复见 `word_lite_repository_test` 自动化用例。）
- 家长页数据与仓库统计一致，换日后「今日」与「连续天数」逻辑符合 4.1、4.4 描述。

------------------------------------------------------------------------

## 7. 开发优先级（参考）

**GitHub 完成度追踪**（勾选与 backlog）：<https://github.com/nancliu/lite-danci/issues/1>

**首页今日学习入口**：已实现；验收清单与说明见 `docs/superpowers/specs/2026-05-12-wordlite-home-entry-design.md` **§7 首页入口实现状态**（主按钮三态、队列预览、进度副文案、完成态、放弃会话、家长入口等）。

**已完成 / 持续维护**：学习流程、状态流转、本地存储、奖励与家长页、**嵌入式词库（内置 + 多文件 JSON 示例词包）**、检查点同日恢复等单测覆盖（与 MVP 范围一致）。

**后续迭代（示例）**：复习抽样策略优化、每会话碎片数值调参、按 `gradeTag` 筛选新词队列、埋点与完成率看板、无障碍与国际化、**应用设置页（见 3.3）**。

------------------------------------------------------------------------

## 8. 总结

核心目标：每天约 5 分钟、低压力完成识记与巩固，用间隔重复把短期记忆推进到稳定掌握；家长端仅观察激励，不替代儿童操作。
